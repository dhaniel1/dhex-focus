import React from "react";
import Button from "../Button";

const TodoItem = () => {
  return (
    <Button className="w-full border-dashed border-amber-800">TodoItem</Button>
  );
};

export default TodoItem;
